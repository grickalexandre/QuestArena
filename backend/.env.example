# Backend
PORT=8080
# Set DEV_MODE=true to force in-memory store + local auth (default when FIREBASE_CREDENTIALS is empty)
DEV_MODE=true
# FIREBASE_CREDENTIALS=./firebase-service-account.json
# FIREBASE_PROJECT_ID=your-project-id
